- @posts.each do |post|
  %h2.font-orange= link_to post.title, post
  %p.font-white
    = "~ Posted " + time_ago_in_words(post.created_at) + " ago"
    by
    = "@" + post.user.username + " ~"
= link_to 'New Post', new_post_path
